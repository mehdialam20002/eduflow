# Brief for 51-data-dictionary-platform-and-people.md

Title: Data Dictionary: Platform and People
Minimum words: 5000
Web research needed: no

## What this chapter must cover (every item, fully)

Table-by-table data dictionary generated by reading these schema files completely: 00-base, 01-platform, 02-auth, 04-people. For EVERY model in those files write an H3 with the table name (snake_case) and: one-line purpose, then a column table (Column | Type | Null | Default | Description including PK/FK/UK marks), then a compact line for indexes and unique constraints, and a line for relations (belongs to / has many). Use the real field names mapped to snake_case columns exactly as in the schema. Group the H3 sections under H2 headings per schema file. No invented columns. Write in many steps (one schema file per step) so nothing is cut off.
