# Brief for 02-working-with-claude-code.md

Title: Working with Claude Code
Minimum words: 3200
Web research needed: no

## What this chapter must cover (every item, fully)

A practical method for building EduFlow with Claude Code (Anthropic's agentic coding tool — terminal CLI, VS Code extension, desktop and web). Install and first run. Put the specs in the repo (docs/canon.md, docs/prd, docs/schema, docs/api, docs/permissions.md per the shared index) so Claude reads the real spec. A complete CLAUDE.md template for the EduFlow repo (project overview, stack, commands, folder map, non-negotiable rules: every query tenant-scoped, response envelope, Zod validation, permission middleware on every route, money as Decimal, no secrets in code, tests for money and auth code, conventional commits) in a markdown code fence. The prompt anatomy used by all 60 prompts (Context to read → Task → Constraints → Files to create/change → Acceptance checks → What to report back). The build loop per module: plan mode first → review plan → implement in small steps → run tests → read the diff → manual test with the checklist → commit. How to review AI-written code: a 15-point checklist (tenant filter present? permission check? validation? error codes? transaction around money writes? N+1 queries? indexes? secrets? tests?). Context management (one module per session, /clear between tasks, keep CLAUDE.md short, use subagents for research). Using Git as a safety net (branch per task, commit before big changes, how to undo). Common failure patterns and the fix prompt for each (invented fields not in schema, skipped tenant filter, giant files, fake tests, outdated library APIs → ask it to check the installed version's docs). Cost and usage tips. Security hygiene (never paste production secrets or real student data into prompts; use .env and seed data). Table: which Claude Code feature to use when (plan mode, slash commands, custom commands in .claude/commands, hooks for lint/test, MCP servers such as Postgres or Playwright). Keep product claims general and accurate; if unsure of an exact command or flag, describe the feature without inventing syntax.
